---
title: "Kinsu Savings App"
slug: "kinsu-savings"
summary: "DeFi savings aggregator built on ConsumerFi protocol"
date: "2024-01-10"
updated: "2025-10-16"
tags: ["defi", "product", "yield", "savings", "web3"]
tech: ["React", "Web3.js", "Solidity", "The Graph", "Node.js"]
cover: ""
status: "production"
demo: "https://kinsu.fi"
featured: false
---

## Overview

Led product vision and execution for a consumer-friendly DeFi savings aggregator, architecting the system design and coordinating cross-functional teams from concept to launch. Simplified yield farming for mainstream users through automated optimization, multi-chain portfolio management, and one-click diversification strategies.

<Terminal cmd="system architecture">
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   React UI   │─────▶│  Smart       │─────▶│  DeFi        │
│   (Web3)     │      │  Contracts   │      │  Protocols   │
└──────────────┘      └──────────────┘      └──────────────┘
       │                     │                      │
       ▼                     ▼                      ▼
  ┌──────────┐          ┌──────────┐          ┌──────────┐
  │ The Graph│          │  Backend │          │ Price    │
  │ Indexer  │          │  API     │          │ Oracles  │
  └──────────┘          └──────────┘          └──────────┘
</Terminal>

<Window title="Smart Contract Strategy">
```solidity
// Automated yield allocation
contract KinsuVault {
  function deposit(uint256 amount) external {
    // Allocate to highest yield protocol
    uint256 bestAPY = findBestYield();
    allocateFunds(bestAPY, amount);

    // Emit event for indexer
    emit Deposit(msg.sender, amount, bestAPY);
  }

  function rebalance() external {
    // Gas-optimized multi-protocol rebalancing
    if (shouldRebalance()) {
      withdrawFromLowYield();
      depositToHighYield();
    }
  }
}
```
</Window>

**Links:**
- [Live Application →](https://kinsu.fi)
